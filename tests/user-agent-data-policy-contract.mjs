import assert from 'node:assert/strict';
import fs from 'node:fs';

const read = path => fs.readFileSync(path,'utf8');
const domain = read('includes/user-agent-system-v236.php');
const usage = read('includes/user-data-usage-v236.php');
const index = read('includes/shared-knowledge-index-v236.php');
const policy = read('includes/chat-agent-policy-v236.php');
const settingsApi = read('api/user-agent-system-v236.php');
const onboardingApi = read('api/chat-onboarding-v241.php');
const chatApi = read('api/chat-v236.php');
const chatStream = read('api/chat-stream-v121.php');
const agentContext = read('agent-context-v131.js');
const chatPage = read('chat.php');
const chatIdentity = read('chat-agent-identity-v236.js');
const account = read('account-agent-settings-v236.js');
const adminAi = read('admin/ai.php');
const adminUsage = read('admin/ai-data-usage-v236.php');
const adminHeader = read('admin/_header.php');
const bootstrap = read('includes/bootstrap.php');
const upgrade = read('upgrade.php');
const sql = read('upgrade-user-agent-system-v236.sql');

assert.match(domain,/function system_agent_name\(\)/,'system agent has one configurable name owner');
assert.match(domain,/setting\('system_agent_name','STONEFELLOW'\)/,'STONEFELLOW remains the system-name fallback');
assert.match(adminAi,/name="system_agent_name"/,'admin can change the universal system agent name');
assert.match(adminAi,/save_setting\('system_agent_name'/,'admin system-agent name persists through settings');

assert.match(domain,/function user_agents_list_v236/,'user agents are explicit stored account objects');
assert.match(domain,/Enter a name for your agent/,'named user agents require the user to supply a name');
assert.doesNotMatch(domain,/\$first\."'s Agent"|Dave's Agent|My Agent/,'canonical agent domain never invents a user-agent name');
assert.match(domain,/onboarding_dismissed/,'user-agent preferences retain canonical onboarding completion state');
assert.match(chatIdentity,/Choose your onboarding experience/,'main Chat begins the complete deterministic onboarding flow');
assert.match(chatIdentity,/Name your agent/,'Chat onboarding includes explicit agent naming');
assert.match(chatIdentity,/data-keep-system/,'Chat onboarding offers the system name as an explicit naming choice');
assert.match(chatIdentity,/chat-onboarding-v241\.php/,'Chat onboarding saves through its deterministic state owner');
assert.match(onboardingApi,/user_agent_create_v236/,'final onboarding save creates the user agent through the canonical domain');
assert.match(account,/\+ Name an Agent/,'Account Settings provides persistent agent creation');

assert.match(domain,/engine_key VARCHAR\(40\) NOT NULL DEFAULT 'stonefellow'/,'named user agents are powered by Stonefellow');
assert.match(domain,/stonefellow_shared TINYINT\(1\) NOT NULL DEFAULT 0/,'system/network data sharing defaults off');
assert.match(domain,/owner_agents_allowed TINYINT\(1\) NOT NULL DEFAULT 1/,'owner agents can have separate private-data permission');
assert.match(domain,/profile_agent_allowed TINYINT\(1\) NOT NULL DEFAULT 0/,'profile-agent data exposure defaults off');
assert.match(domain,/connections.*collaborators.*public/s,'audience model includes relationship and public scopes');
assert.match(domain,/user_agent_data_rules/,'per-agent resource overrides are first-class');

assert.match(chatPage,/api\/chat-v236\.php/,'live Chat shell routes through the scoped v236 API');
assert.match(chatPage,/chat-agent-identity-v236\.js/,'live Chat loads optional agent identity/onboarding runtime');
assert.match(chatPage,/user_agent_id IS NULL/,'system Chat resumes only system-agent conversations');
assert.match(chatPage,/user_agent_id=\?/,'named-agent Chat resumes only that agent’s conversations');
assert.match(chatApi,/chat_v236_scope_sql/,'Chat API has one conversation-scope owner');
assert.match(chatApi,/user_agent_id=\?/,'named-agent API conversations are scoped by agent ID');
assert.match(chatApi,/user_agent_id IS NULL/,'system API conversations cannot leak into named-agent history');
assert.match(chatApi,/chat_generate_answer_policy_v236\(\$query,\$history,\$user,\$principal,\$agentContext,\$conversationId\)/,'AI retrieval audit receives the active conversation ID');

assert.match(agentContext,/user_agent_id:activeUserAgentId\(\)/,'shared Agent Context carries the selected named agent into voice transport');
assert.match(agentContext,/STONEFELLOW_AGENT_IDENTITY_V236\?\.agentId/,'Agent Context reads the canonical selected agent identity');
assert.ok(agentContext.includes("const conversationKey=`stonefellow:conversation-id:${userId}:${selectedUserAgentId||'system'}`"),'stored conversation continuity is isolated by user and selected agent');
assert.match(chatStream,/function chat_v121_scope_sql\(/,'voice Chat has the same explicit conversation scope model as text Chat');
assert.match(chatStream,/user_agent_id=\?/,'voice Chat can require a named-agent conversation');
assert.match(chatStream,/user_agent_id IS NULL/,'voice Chat keeps system conversations isolated from named agents');
assert.match(chatStream,/\$rawContext\['user_agent_id'\]/,'voice transport consumes the selected agent from canonical Agent Context');
assert.match(chatStream,/user_agent_get_v236\(\$pdo,\$userId,\$requested\)/,'voice Chat validates selected agent ownership');
assert.match(chatStream,/user_agent_principal_v236\(\$user,\$activeAgent\)/,'voice Chat uses the selected agent principal rather than forcing the system principal');
assert.match(chatStream,/INSERT INTO chat_conversations \(user_id,user_agent_id,artist_workspace_id,title\)/,'new voice conversations persist the selected agent owner');
assert.match(chatStream,/chat_v121_owned\(\$pdo,\$conversationId,\$userId,\$activeAgent\)/,'existing voice conversations are checked against selected-agent scope');
assert.match(chatStream,/chat_v121_context\(\$query,\$user,\$principal,\$agentContext,\$conversationId\)/,'voice retrieval receives the selected agent principal');
assert.match(chatStream,/'agent'=>\['id'=>\$agentScopeId/,'voice assistant messages persist their agent identity');
assert.match(chatStream,/'agent_name'=\>\(string\)\$principal\['display_name'\]/,'voice responses report the selected agent name');

assert.match(index,/CREATE TABLE IF NOT EXISTS shared_knowledge_index/,'shared knowledge has a dedicated discovery index');
assert.match(index,/knowledge_id INT UNSIGNED NOT NULL PRIMARY KEY/,'shared index points to the authoritative knowledge item');
assert.match(index,/owner_user_id INT UNSIGNED NOT NULL/,'shared index retains source ownership');
assert.match(index,/source_version_hash CHAR\(64\)/,'shared index detects source changes');
assert.match(index,/topic_tags VARCHAR\(1000\)/,'shared index stores discovery metadata');
assert.match(index,/embedding_ref VARCHAR\(190\)/,'shared index has a future semantic-search pointer without embedding raw knowledge in this phase');
const createTable = index.match(/CREATE TABLE IF NOT EXISTS shared_knowledge_index \([\s\S]*?\) ENGINE=/)?.[0] || '';
assert.ok(createTable,'shared index schema block is inspectable');
assert.doesNotMatch(createTable,/content_text|chunk_text|knowledge_body|raw_content/,'shared index does not duplicate source knowledge or chunks');
assert.match(index,/user_data_policy_get_v236\(\$pdo,\$owner,'knowledge'/,'index sync uses current owner sharing policy');
assert.match(index,/revoked_at=COALESCE\(revoked_at,NOW\(\)\)/,'revoked sharing disables the index pointer');
assert.match(policy,/shared_knowledge_index_candidates_v236/,'cross-user knowledge discovery starts from the shared index');
assert.match(policy,/shared_knowledge_index_item_v236/,'retrieval fetches the current authoritative source after discovery');
assert.match(policy,/shared_knowledge_index_hash_v236/,'retrieval revalidates the source version');
assert.match(policy,/chat_policy_can_use_v236/,'live authorization is checked again before context use');
assert.match(policy,/WHERE created_by_user_id=\? AND knowledge_scope='personal'/,'direct personal Knowledge retrieval is owner-scoped');
const sharedPersonalPolicy = policy.match(/function chat_policy_shared_personal_knowledge_v242\([\s\S]*?\n}\n\nfunction chat_policy_system_knowledge_v242/)?.[0] || '';
assert.ok(sharedPersonalPolicy,'cross-user personal Knowledge policy is inspectable');
assert.match(sharedPersonalPolicy,/shared_knowledge_index_candidates_v236/,'cross-user personal Knowledge discovery uses the shared pointer index');
assert.doesNotMatch(sharedPersonalPolicy,/FROM knowledge_items|JOIN knowledge_items/,'cross-user personal Knowledge policy cannot raw-scan the Knowledge table');

assert.match(usage,/CREATE TABLE IF NOT EXISTS user_data_retrieval_log/,'actual AI context use has a dedicated retrieval ledger');
assert.match(usage,/owner_user_id/,'ledger records data owner');
assert.match(usage,/requester_user_id/,'ledger records requester account for admin accountability');
assert.match(usage,/agent_name_snapshot/,'ledger records the agent identity used');
assert.match(usage,/resource_type/,'ledger records resource attribution');
assert.match(usage,/conversation_id/,'ledger can tie an event to the internal conversation without copying prompt text');
const usageTable = usage.match(/CREATE TABLE IF NOT EXISTS user_data_retrieval_log \([\s\S]*?\) ENGINE=/)?.[0] || '';
assert.doesNotMatch(usageTable,/prompt|query_text|message_text|conversation_text|request_body/,'retrieval ledger does not duplicate prompts or conversation text');
assert.match(policy,/user_data_usage_log_v236/,'ledger writes happen when authorized data is actually appended to AI context');
assert.match(settingsApi,/data_usage/,'owner Account state includes data-usage telemetry');
assert.match(account,/Shared Data Usage/,'data owner can inspect AI use of their content');
assert.match(account,/requester’s conversation text are not copied/,'owner UI explains the transparency boundary');
assert.match(adminUsage,/AI Data Usage/,'admin has a network-wide AI data usage page');
assert.match(adminUsage,/requester_name/,'admin ledger can attribute requester accounts');
assert.match(adminHeader,/AI Data Usage/,'admin navigation exposes the audit view');

assert.match(settingsApi,/shared_knowledge_index_sync_owner_v236/,'knowledge sharing changes refresh the discovery index');
assert.match(settingsApi,/refresh_shared_knowledge/,'owner can explicitly refresh their shared index');
assert.match(account,/Shared Knowledge Index/,'owner sees index state without a copied KB view');
assert.match(bootstrap,/shared-knowledge-index-v236\.php/,'shared knowledge index is part of canonical bootstrap');
assert.match(upgrade,/user_agent_system_ensure_schema_v236\(\)/,'normal upgrade installs user-agent schema');
assert.match(upgrade,/user_data_usage_ensure_schema_v236\(\)/,'normal upgrade installs retrieval ledger');
assert.match(upgrade,/shared_knowledge_index_ensure_schema_v236\(\)/,'normal upgrade installs shared knowledge index');
assert.match(sql,/shared_knowledge_index/,'standalone schema documents shared knowledge index storage');
assert.match(sql,/user_data_retrieval_log/,'standalone schema documents retrieval audit storage');

assert.equal(fs.existsSync('includes/user-agent-data-policy.php'),false,'superseded auto-naming policy implementation is removed');
assert.equal(fs.existsSync('api/user-agent-data-policy.php'),false,'superseded policy API is removed');
assert.equal(fs.existsSync('agent-data.php'),false,'superseded standalone Agent & Data page is removed');
assert.equal(fs.existsSync('agent-data.css'),false,'superseded standalone Agent & Data styles are removed');

console.log('USER_AGENT_DATA_POLICY_CONTRACT=PASS');