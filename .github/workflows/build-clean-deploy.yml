name: Build Clean Deploy

on:
  push:
    branches:
      - deploy/package-4eecaddc
    paths:
      - .github/workflows/build-clean-deploy.yml

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact source
        uses: actions/checkout@v4

      - name: Build runtime package
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p deploy
          rsync -a ./ deploy/ \
            --exclude='.git/' \
            --exclude='.github/' \
            --exclude='tests/' \
            --exclude='tools/' \
            --exclude='RECOVERY_RUNTIME_BASELINE.md' \
            --exclude='recovery-inventory.generated.md' \
            --exclude='recovery-inventory.generated.json' \
            --exclude='admin/force-cache-repair-20260825.php'
          printf '%s\n' 'Source commit: 4eecaddcca85238271c0a590867ea4d9167275b0' > deploy/DEPLOY_SOURCE.txt
          test -f deploy/.htaccess

      - name: Upload clean deploy ZIP
        uses: actions/upload-artifact@v4
        with:
          name: software-deploy
          path: deploy/
          if-no-files-found: error
          include-hidden-files: true
          compression-level: 6
