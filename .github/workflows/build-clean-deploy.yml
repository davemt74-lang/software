name: Build Clean Deploy

on:
  push:
    branches:
      - deploy/package-0bd4d687
    paths:
      - .github/workflows/build-clean-deploy.yml

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact source
        uses: actions/checkout@v4

      - name: Build runtime package
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p deploy
          rsync -a ./ deploy/ \
            --exclude='.git/' \
            --exclude='.github/' \
            --exclude='tests/' \
            --exclude='tools/' \
            --exclude='RECOVERY_RUNTIME_BASELINE.md' \
            --exclude='recovery-inventory.generated.md' \
            --exclude='recovery-inventory.generated.json' \
            --exclude='admin/force-cache-repair-20260825.php'
          printf '%s\n' 'Source commit: 0bd4d6877e3aabe7c4a7fca9c0b8a8b04f10d911' > deploy/DEPLOY_SOURCE.txt

      - name: Upload clean deploy ZIP
        uses: actions/upload-artifact@v4
        with:
          name: software-deploy-0bd4d687
          path: deploy/
          if-no-files-found: error
          compression-level: 6
