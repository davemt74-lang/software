name: Agent Scheduling v4.30

on:
  push:
    branches:
      - main
      - feature-agent-scheduling-phase-1
  pull_request:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Agent Scheduling contract
        run: node tests/agent-scheduling-v430-contract.mjs

      - name: JavaScript syntax
        run: node --check tests/agent-scheduling-v430-contract.mjs

      - name: PHP syntax
        run: |
          php -l includes/agent-scheduling-v430.php
          php -l includes/bootstrap.php
          php -l upgrade.php
