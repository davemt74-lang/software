Stonefellow private runtime storage.
ai-key.php is generated automatically the first time an API key is saved.
commerce-delivery/ is created at runtime for receipt-gated Profile Commerce delivery files.
The private directory is denied to direct HTTP access; Commerce files must be streamed only through the authorized PHP delivery endpoint.
Back up private runtime data with the site configuration. Never publish or share it.
